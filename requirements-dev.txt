# ============================================================
# Thư viện phục vụ KIỂM THỬ (không cần khi chạy website)
# Cài bằng:  pip install -r requirements-dev.txt
# ============================================================

# ===== Giai đoạn 1: Unit Test =====
# Ghim nhánh pytest 8.x: pytest-playwright (Giai đoạn 2) yêu cầu pytest<9.0.0
pytest==8.3.5
pytest-django==4.14.0
pytest-cov==7.1.0

# ===== Giai đoạn 2: E2E Test (Playwright) =====
# Sau khi cài, chạy thêm:  playwright install chromium
pytest-playwright==0.7.1
pytest-base-url==2.1.0

# ===== Tiện ích =====
pytest-xdist==3.6.1        # chạy test song song:  pytest -n auto

# ===== Giai đoạn 3: AI phân tích log lỗi =====
pytest-json-report==1.5.0   # xuất kết quả pytest ra JSON có cấu trúc
openai==3.8.0               # SDK gọi DeepSeek (giao thức tương thích OpenAI)
